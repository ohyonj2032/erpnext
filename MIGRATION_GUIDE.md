# Sales Order 架构重构迁移指南

## 概述

本文档描述了如何将现有的 Sales Order 提交流程从紧耦合架构迁移到新的事件驱动、服务导向架构。

## 新架构概览

### 架构层次

```
┌─────────────────────────────────────────────────────────────┐
│                    表现层 (DocType)                         │
│              sales_order.py (on_submit)                     │
└────────────────────────────┬────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────┐
│              应用服务层 (Application Service)               │
│          erpnext.selling.services.SalesOrderService         │
│           (唯一编排入口，负责流程协调)                       │
└────────────────────────────┬────────────────────────────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
┌───────▼───────┐    ┌──────▼───────┐    ┌──────▼───────┐
│  库存服务    │    │  会计服务    │    │  通知服务    │
│  (Domain)    │    │  (Domain)    │    │  (Domain)    │
└───────────────┘    └──────────────┘    └──────────────┘
        │                    │                    │
        └────────────────────┼────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────┐
│                  事件总线 (Event Bus)                       │
│              (发布/订阅，松耦合集成)                         │
└─────────────────────────────────────────────────────────────┘
```

### 核心组件

1. **事件系统** (`erpnext/events/`)
   - `events.py`: 定义所有领域事件
   - `event_bus.py`: 实现发布/订阅模式
   - `handlers.py`: 事件处理器基类
   - `listeners.py`: 事件监听器

2. **应用服务** (`erpnext/selling/services/`)
   - `SalesOrderService`: 唯一编排入口，负责协调所有子流程

3. **领域服务**
   - `StockReservationService`: 库存预留逻辑
   - `SalesInvoiceService`: 会计处理逻辑
   - `NotificationService`: 通知发送逻辑

## 迁移步骤

### 阶段 1: 准备（0风险）

1. **部署新代码**
   - 所有新文件都是增量添加，不会影响现有功能
   - 新架构默认关闭，通过设置控制

2. **创建功能开关设置**
   ```python
   # 在 Selling Settings 中添加:
   # - "Use New Sales Order Architecture" (复选框，默认关闭)
   ```

### 阶段 2: 测试验证（低风险）

1. **在测试环境启用新架构**
   - 进入 Selling Settings
   - 勾选 "Use New Sales Order Architecture"

2. **执行全面测试**
   - 测试正常的 Sales Order 提交流程
   - 测试库存预留功能
   - 测试取消功能
   - 测试边界条件（库存不足、网络错误等）

3. **验证补偿机制**
   - 模拟会计过账失败，验证库存预留是否正确回滚
   - 检查日志确保所有操作都有记录

### 阶段 3: 灰度发布（中风险）

1. **选择试点用户/部门**
   - 为特定用户组启用新架构
   - 或者在特定时间段启用

2. **监控关键指标**
   - 错误率
   - 响应时间
   - 事务成功率

3. **保留回滚能力**
   - 随时可以通过设置切换回旧架构

### 阶段 4: 全面推广（低风险）

1. **逐步扩大范围**
   - 根据监控结果，逐步推广到所有用户

2. **完全启用新架构**
   - 确认无误后，在生产环境完全启用

3. **移除旧代码（可选）**
   - 稳定运行一段时间后，可以考虑移除旧的实现
   - 保留 `_on_submit_legacy` 作为备份

## 补偿机制详解

### 工作原理

```python
# 在 SalesOrderService.submit_sales_order 中:

1. 记录每个成功执行的步骤到 compensation_log
2. 如果后续步骤失败，按相反顺序执行补偿操作
3. 使用数据库事务确保一致性
```

### 补偿操作映射

| 原始操作 | 补偿操作 | 说明 |
|---------|---------|------|
| `reserve_stock` | `cancel_reservations` | 取消所有库存预留 |
| `post_gl` | `cancel_gl_entries` | 冲销会计分录 |

### 异常处理

- 所有补偿操作都在 try-except 中执行
- 补偿失败会记录日志但不会中断流程
- 提供手动修复的可能性

## 关键设计决策

### 1. 为什么选择应用服务作为唯一编排入口？

**原因：**
- 单一职责原则：一个地方负责整个业务流程
- 易于维护和修改：流程变更只需修改一个地方
- 便于测试：可以独立测试整个流程
- 保持 Doctype 简洁：DocType 只负责数据验证和基础操作

### 2. 为什么使用事件驱动架构？

**原因：**
- 解耦：子系统之间通过事件通信，不直接依赖
- 可扩展性：添加新的响应者只需订阅事件
- 异步处理：通知等非关键路径可以异步执行
- 可审计性：所有事件都可以被记录和回放

### 3. 如何处理数据一致性？

**策略：**
- 补偿模式：失败时回滚已执行的操作
- 数据库事务：使用 frappe 的事务机制
- 幂等性设计：确保重复执行不会产生问题
- 日志记录：所有操作都有详细日志

## 目录结构变更

### 新增文件

```
erpnext/
├── events/                          # 新事件系统
│   ├── __init__.py
│   ├── events.py                    # 事件定义
│   ├── event_bus.py                 # 事件总线
│   ├── handlers.py                  # 处理器基类
│   └── listeners.py                 # 事件监听器
├── selling/
│   └── services/                    # 新应用服务层
│       ├── __init__.py
│       └── sales_order_service.py
├── stock/
│   └── services/                    # 新库存领域服务
│       ├── __init__.py
│       └── stock_reservation_service.py
├── accounts/
│   └── services/                    # 新会计领域服务
│       ├── __init__.py
│       └── sales_invoice_service.py
└── notifications/                   # 新通知模块
    ├── __init__.py
    └── services/
        ├── __init__.py
        └── notification_service.py
```

### 修改文件

```
erpnext/selling/doctype/sales_order/sales_order.py
- 添加 _on_submit_legacy()
- 添加 _on_submit_new_architecture()
- 修改 on_submit() 使用新的分支逻辑
- 类似地修改 on_cancel()
```

## 回滚计划

### 快速回滚步骤

1. **在 Selling Settings 中关闭新架构**
   - 取消勾选 "Use New Sales Order Architecture"
   - 保存设置

2. **立即生效**
   - 所有新的 Sales Order 提交会自动使用旧逻辑
   - 已提交的订单不受影响

### 数据回滚

- 新架构创建的数据与旧架构兼容
- 无需特殊的数据迁移或回滚

## 监控和维护

### 关键日志

```
# 事件日志
- "Sales Order {name} submitted successfully"
- "Stock reserved for {doc_type} {doc_name}"
- "GL entries posted for {doc_type} {doc_name}"

# 错误日志
- "Sales Order Submission Failed"
- "Stock Reservation Failed"
- "Compensation Failed: {action}"
```

### 健康检查

建议定期检查：
1. 错误日志中的异常
2. 补偿操作的执行次数
3. 事务成功率

## 最佳实践

1. **渐进式迁移**：不要一次性切换所有功能
2. **保持向后兼容**：旧代码保留足够长时间
3. **充分测试**：特别是补偿逻辑
4. **监控指标**：建立关键指标的监控
5. **文档化**：保持本文档更新

## 常见问题

### Q: 新架构会影响性能吗？

A: 新架构增加了一层抽象，但影响很小。异步处理（如通知）可能会提升用户体验。建议在测试环境进行性能对比。

### Q: 如果补偿操作也失败了怎么办？

A: 补偿操作失败会记录详细日志。系统设计为最终一致性，可以通过后台任务或手动修复来处理。

### Q: 如何扩展新的子流程？

A: 在 SalesOrderService 中添加新的步骤，创建对应的领域服务，并确保实现补偿逻辑。

## 附录

### 相关文档

- ERPNext 官方文档
- Frappe 框架文档
- 领域驱动设计 (DDD) 相关资料

### 联系人

如有问题，请联系架构团队。
